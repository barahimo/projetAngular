export interface Teacher {
    id?: string,
    nom?: string,
    prenom?: string,
    sexe?: string,
    dateAffectation?: string,
    adresse?: string,
    massar?: string,
    image?: string,
    //getSalary: (base: number) => number;
}